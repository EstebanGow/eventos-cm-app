export enum IArchivo {
    XLSX = 'eventos.xlsx',
}

export enum CamposDireccion {
    PAIS = 0,
    CIUDAD = 1,
    DIRECCION = 2,
}

export enum EstadoProcesoArchivo {
    PROCESANDO = 2,
    FALLIDO = 3,
    FINALIZADO = 4,
}
